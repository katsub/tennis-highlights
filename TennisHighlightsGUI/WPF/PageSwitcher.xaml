<Window x:Class="TennisHighlightsGUI.PageSwitcher"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Tennis Highlights"
        MinHeight="480"
        Width="{Binding Width, Mode=TwoWay, FallbackValue=1280}"
        MinWidth="{Binding Width, Mode=OneWay, FallbackValue=1280}"
        MaxWidth="{Binding Width, Mode=OneWay, FallbackValue=1280}"
        Height="{Binding Height, Mode=TwoWay, FallbackValue=720}">
    <Grid>
    </Grid>
</Window>
