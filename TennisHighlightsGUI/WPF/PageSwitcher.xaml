<Window x:Class="TennisHighlightsGUI.PageSwitcher"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Tennis Highlights"
        MinHeight="480"
        Width="{Binding Width, Mode=TwoWay, FallbackValue=1920}"
        MinWidth="{Binding Width, Mode=OneWay, FallbackValue=1920}"
        MaxWidth="{Binding Width, Mode=OneWay, FallbackValue=1920}"
        Height="{Binding Height, Mode=TwoWay, FallbackValue=1080}">
    <Grid>
    </Grid>
</Window>
